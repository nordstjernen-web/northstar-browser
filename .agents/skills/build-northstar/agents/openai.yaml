interface:
  display_name: "Build Cross-Platform Browser"
  short_description: "Build a C browser across desktop platforms"
  default_prompt: "Use $build-northstar to compile and smoke-test this browser on the current operating system."
